# Modbus Control Center

The `modbus-ctrl-center` package provides a centralized UI or API layer that builds upon the core engine, allowing broader oversight and management of multiple Modbus devices.

## Purpose

- Acts as a high-level orchestration component.
- Manages the lifecycle of multiple connections simultaneously.
- Exposes device configurations to visual interfaces or external integrations.
