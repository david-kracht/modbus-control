# Modbus Control Simulation

The `modbus-ctrl-simulation` package provides mock or simulation environments to develop, test, and debug Modbus integrations without requiring physical hardware.

## Purpose

- Simulates generic or schema-specific device responses.
- Allows test-driven development (TDD) for Modbus control logic.
- Eases continuous integration setups by providing reliable mock data.
